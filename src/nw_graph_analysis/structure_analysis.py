from structure_extraction import *

# get junc patch: location and 3x3 nbrhood
# obtain reference junctions and per frame junctions
# classify junctions into isolated and fuzzy and unk


# start with per frame skel and reference frame skel
# Get per frame graph and reference frame graph

# Create junction projection frame and apply conn comp analysis to get the labelled areas



# label junctions as isolated, fuzzy, unk

# For (region, replicate, channel) get mean, std over sequence in CC mean values
# Get correlation between channels for (region, replicate, channel)

# From projection frame, get CC area: replicates and all

#